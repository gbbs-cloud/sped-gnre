version: "3.8"

services:
  app:
    build:
      context: .
    ports:
      - '9000:9000'
    volumes:
      - ./:/var/www
    command: php-fpm

  nginx:
    image: nginx:stable-alpine
    ports:
      - '80:80'
    volumes:
      - ./:/var/www
      - ./nginx/default.conf:/etc/nginx/conf.d/default.conf
    depends_on:
      - app